<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="openMenu_Click"/>
                <MenuItem Header="_Save" />
                <Separator />
                <MenuItem Header="_Exit" />
                <Grid Height="100" Width="100"/>
            </MenuItem>
        </Menu>

        <ComboBox x:Name="comboBox" Height="279" VerticalAlignment="Top" Width="120"/>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Name="lblCursorPosition" />
            </StatusBarItem>
            <Separator Grid.Column="1" />
            <StatusBarItem Grid.Column="2">
                <TextBlock Text="c:\path\of\current\file.txt" />
            </StatusBarItem>
            <Separator Grid.Column="3" />
            <StatusBarItem Grid.Column="4">
                <ProgressBar Value="50" Width="90" Height="16" />
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="2">
            <DataGrid Name="comparedDataGrid" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Date Object A" Binding="{Binding StringDateA}" />
                    <DataGridTextColumn Header="Time Object A" Binding="{Binding StringTimeA}" />
                    <DataGridTextColumn Header="Version List A" Binding="{Binding VersionListA}" />
                    <DataGridTextColumn Header="No Of Lines Object A" Binding="{Binding NoOfLinesA}" />
                    <DataGridTextColumn Header="Date Object B" Binding="{Binding StringDateB}" />
                    <DataGridTextColumn Header="Time Object B" Binding="{Binding StringTimeB}" />
                    <DataGridTextColumn Header="Version List B" Binding="{Binding VersionListB}" />
                    <DataGridTextColumn Header="No Of Lines Object B" Binding="{Binding NoOfLinesB}" />
                    <DataGridTextColumn Header="Difference Comment" Binding="{Binding Difference}" />
                </DataGrid.Columns>
                <!--<DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Details}" Margin="10" />
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>-->
            </DataGrid>
        </Grid>
        
    </DockPanel>
</Window>
